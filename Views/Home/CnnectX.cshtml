@* @model Unique.Models.WhitelistEntry
@{
    var isVerified = ViewBag.TwitterVerified as bool? ?? false;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Connect X & Apply for Whitelist</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            background: linear-gradient(to bottom, #ffffff, #f0f4f8);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container-box {
            background: #ffffff;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            padding: 40px;
            max-width: 500px;
            width: 90%;
            text-align: center;
            animation: fadeIn 0.6s ease-in-out;
        }

        .connect-btn {
            padding: 12px 24px;
            background-color: #1DA1F2;
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s ease;
            margin-bottom: 30px;
        }

            .connect-btn:hover {
                background-color: #0d8ddb;
            }

        input, button {
            width: 100%;
            padding: 12px;
            margin-bottom: 15px;
            border-radius: 10px;
            border: 2px solid #1da1f2;
            font-size: 1rem;
        }

            button.submit-btn {
                background: linear-gradient(45deg, #ff6fb1, #c471ed);
                border: none;
                color: white;
                font-weight: bold;
                cursor: pointer;
                transition: background 0.4s, transform 0.2s;
            }

                button.submit-btn:hover {
                    background: linear-gradient(45deg, #c471ed, #ff6fb1);
                    transform: scale(1.03);
                }

        .locked-message {
            font-size: 1.2rem;
            margin-top: 20px;
            color: #666;
        }

        .text-danger {
            color: red;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="container-box">
        @if (!isVerified)
        {
            <!-- Show Connect button only -->
            <img src="/Images/x.jpg" alt="Twitter Logo" style="width:100px; margin-bottom:20px;" />
            <h2>Connect your X (Twitter) to proceed</h2>
            <button class="connect-btn" onclick="window.location.href='@Url.Action("Login", "Twitter")'">Connect with X</button>
            <p class="locked-message">Please connect your Twitter account to unlock the application form.</p>
        }
        else
        {
            <!-- Show the whitelist form -->
            <h1>Apply For Whitelist ✨</h1>

            <div asp-validation-summary="All" class="text-danger"></div>
            <input asp-for="TweetUrl" type="url" placeholder="Your Tweet URL (proof)" required />
            <span asp-validation-for="TweetUrl" class="text-danger"></span>

            <input asp-for="ReferralCodeInput" type="text" placeholder="Referral Code (Optional)" />
            <span asp-validation-for="ReferralCodeInput" class="text-danger"></span>

            <form asp-action="Apply" method="post" asp-antiforgery="true">
              
              
            </form>
        }
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.5/jquery.validate.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validation-unobtrusive/3.2.13/jquery.validate.unobtrusive.min.js"></script>
</body>
</html>
 *@